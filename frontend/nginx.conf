server {
    listen 80;
    server_name localhost;

    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
        
        # Esta es la directiva clave que redirige el tráfico
        try_files $uri $uri/ /index.html;
    }
}